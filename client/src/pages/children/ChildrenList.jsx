import React from 'react';
import PlaceholderView from '../../components/ui/PlaceholderView';

const ChildrenList = () => (
  <PlaceholderView
    title="Children Directory"
    description="Anonymized registry of children needing educational, medical, and developmental support."
    moduleName="Children Module"
  />
);

export default ChildrenList;
