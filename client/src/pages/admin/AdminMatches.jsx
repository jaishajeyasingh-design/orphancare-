import React from 'react';
import PlaceholderView from '../../components/ui/PlaceholderView';

const AdminMatches = () => (
  <PlaceholderView
    title="AI Match Approvals"
    description="Review AI-suggested pairings between child needs and donor/volunteer opportunities."
    moduleName="Admin / Matches"
  />
);

export default AdminMatches;
